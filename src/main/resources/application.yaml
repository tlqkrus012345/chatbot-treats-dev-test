server:
  port: ${SPRING_PORT}
  servlet:
    session:
      cookie:
        same-site: None
        secure: true
      context-path: /
      encoding:
        charset: UTF-8
        force-response: true
#
#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://sharetreats-database.cxyn6whvc7o6.ap-northeast-2.rds.amazonaws.com:3306/st
#    username: ${RDS_USERNAME}
#    password: ${RDS_PASSWORD}
#  jpa:
#    hibernate:
#      ddl-auto: update
spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
cloud:
  aws:
    s3:
      bucket: stchatbotimagebucket
    credentials:
      accesskey: AKIAU7PORYUTRMIUE7XU
      secretkey: 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
    region:
      static: ap-northeast-2
    stack:
      auto: false
#
#brand-path: "brand"
#product-path: "product"

logging:
  level:
    org:
      springframework:
        web: DEBUG
        orm:
          jpa: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
